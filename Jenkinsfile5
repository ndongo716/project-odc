pipeline {
    agent any
    stages {
        stage('SCM') {
            checkout scm
        }
        stage("Quality Gate 2") {
            steps {
                waitForQualityGate abortPipeline: true
            }
        }
    }
}
