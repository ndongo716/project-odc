pipeline {
    agent any
    stages {
        stage('SCM') {
            steps {
                checkout scm
            }
        }
        stage("Quality Gate 2") {
            steps {
                withSonarQubeEnv() {
                    waitForQualityGate abortPipeline: true
                }
            }
        }
    }
}
